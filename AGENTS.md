# SQL Agent Guidelines

- Use `list_tables()` to discover available tables  
- Use `get_schema(table)` before writing SQL queries when needed
- Do not guess table or column names  
- Only query tables that exist in the database  
- Keep queries simple and avoid unnecessarily large result sets  